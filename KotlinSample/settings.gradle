pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        maven {
            name = "GitHubPackages"
            url = uri("https://maven.pkg.github.com/dev-adwhale/AdWhaleAndroid/SDK")
            credentials {
                username = 'dev-adwhale'
                password = 'AdWhale SDK 가이드 내 키 값 참조'
            }
        }

        maven {
            url "s3://repo.cauly.net/releases"
            credentials(AwsCredentials) {
                accessKey "AdWhale SDK 가이드 내 키 값 참조"
                secretKey "AdWhale SDK 가이드 내 키 값 참조"
            }
        }
        maven {
            url 'https://artifact.bytedance.com/repository/pangle/'
        }
        maven {
            url 'https://dl-maven-android.mintegral.com/repository/mbridge_android_sdk_oversea'
        }
        maven { url 'https://devrepo.kakao.com/nexus/content/groups/public/' }


    }
}

rootProject.name = "AdWhaleKotlinSampleApp"
include ':app'
